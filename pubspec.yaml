name: magi_carre
description: "Jeu de stratégie traditionnel togolais"
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.14.0-68.0.dev

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
